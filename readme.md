# React Boilerplate

```
npm init -y 
npm install --save-dev webpack webpack-cli

npm run build (anti gia node_modules/.bin/webpack) --> run by default in production mode, making the output bundle minified (or set it at webpack.config.js)
npm run build -- --mode development
```
